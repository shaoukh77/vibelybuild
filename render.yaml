services:
  - type: web
    name: vibelybuild
    env: node
    branch: main
    buildCommand: "npm install && npx next build"
    startCommand: "npx next start -p $PORT"
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000
