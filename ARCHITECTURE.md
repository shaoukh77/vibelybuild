# 🏗️ VibelyBuild.AI - System Architecture

## 📊 High-Level Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    VibelyBuild.AI Platform                   │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   Frontend   │◄───┤  API Routes  ├───►│   Firebase   │  │
│  │  (Next.js)   │    │              │    │   Backend    │  │
│  │ Glassmorphic │    │ AI Router    │    │              │  │
│  │     UI       │    │              │    │  Auth + DB   │  │
│  └──────────────┘    └───────┬──────┘    └──────────────┘  │
│                              │                               │
│                      ┌───────▼────────┐                      │
│                      │   AI System    │                      │
│                      │  Master Router │                      │
│                      └───────┬────────┘                      │
│                              │                               │
│              ┌───────────────┼───────────────┐              │
│              │               │               │              │
│         ┌────▼─────┐   ┌────▼─────┐   ┌────▼─────┐         │
│         │  OpenAI  │   │ Anthropic│   │  Gemini  │         │
│         │ Adapter  │   │  Adapter │   │  Adapter │         │
│         └──────────┘   └──────────┘   └──────────┘         │
│                              │                               │
│                         ┌────▼─────┐                         │
│                         │   Groq   │                         │
│                         │  Adapter │                         │
│                         └──────────┘                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 AI Request Flow

### Step-by-Step: User Builds an App

```
1. USER INPUT
   ┌────────────────────┐
   │ User types prompt: │
   │ "Todo list app"    │
   └─────────┬──────────┘
             │
             ▼
2. FRONTEND
   ┌────────────────────┐
   │ Build page sends   │
   │ POST /api/build    │
   └─────────┬──────────┘
             │
             ▼
3. API ROUTE
   ┌────────────────────┐
   │ Creates build doc  │
   │ in Firestore       │
   └─────────┬──────────┘
             │
             ▼
4. STREAM ROUTE
   ┌────────────────────┐
   │ GET /api/build/    │
   │ stream?id=...      │
   └─────────┬──────────┘
             │
             ▼
5. AI ROUTER (index.js)
   ┌────────────────────┐
   │ Reads config.js    │
   │ provider: 'openai' │
   └─────────┬──────────┘
             │
             ▼
6. ADAPTER (openai.js)
   ┌────────────────────┐
   │ Calls OpenAI API   │
   │ with streaming     │
   └─────────┬──────────┘
             │
             ▼
7. STREAMING RESPONSE
   ┌────────────────────┐
   │ "🔧 Initializing"  │
   │ "📦 Installing..."│
   │ "✅ Complete!"    │
   └─────────┬──────────┘
             │
             ▼
8. FRONTEND UPDATES
   ┌────────────────────┐
   │ Shows logs in      │
   │ real-time on UI    │
   └────────────────────┘
```

---

## 🎛️ Configuration System

### Single Source of Truth

```javascript
// src/lib/ai/config.js

export const AI_CONFIG = {
  CODE_GENERATION: {
    provider: 'openai',  // ← CHANGE THIS
    model: 'gpt-4',      // ← CHANGE THIS
  }
}
```

### How It Works

```
┌─────────────────────────────────────────────────┐
│              Configuration Flow                  │
├─────────────────────────────────────────────────┤
│                                                  │
│  1. App imports from @/lib/ai                   │
│     import { generateCode } from '@/lib/ai'     │
│                                                  │
│  2. Router reads config.js                      │
│     const config = getCurrentProvider()         │
│     → { provider: 'openai', model: 'gpt-4' }   │
│                                                  │
│  3. Router selects adapter                      │
│     const adapter = ADAPTERS[provider]          │
│     → ADAPTERS['openai']                        │
│                                                  │
│  4. Calls adapter function                      │
│     adapter.generateCode({ prompt, model })     │
│                                                  │
│  5. Adapter calls external API                  │
│     openai.chat.completions.create(...)         │
│                                                  │
│  6. Returns result to app                       │
│     ← Generated code                            │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## 📦 Adapter Architecture

### Each Adapter Implements Same Interface

```javascript
// All adapters must implement:

interface AIAdapter {
  generateText(options): Promise<string>
  generateStreamingText(options): AsyncGenerator<string>
  generateCode(options): Promise<string>
  generateStreamingCode(options): AsyncGenerator<string>
  validateApiKey(): Promise<ValidationResult>
}
```

### Provider-Specific Implementation

```
┌──────────────────────────────────────────┐
│          OpenAI Adapter                   │
├──────────────────────────────────────────┤
│ import OpenAI from 'openai'              │
│                                           │
│ const client = new OpenAI({              │
│   apiKey: process.env.OPENAI_API_KEY    │
│ })                                        │
│                                           │
│ async function generateText() {          │
│   return client.chat.completions...     │
│ }                                         │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│        Anthropic Adapter                  │
├──────────────────────────────────────────┤
│ import Anthropic from '@anthropic-ai/sdk'│
│                                           │
│ const client = new Anthropic({           │
│   apiKey: process.env.ANTHROPIC_API_KEY │
│ })                                        │
│                                           │
│ async function generateText() {          │
│   return client.messages.create...      │
│ }                                         │
└──────────────────────────────────────────┘
```

---

## 🔐 Security Architecture

### Environment Variables

```
┌─────────────────────────────────────────┐
│         .env.local (Server-Side)         │
├─────────────────────────────────────────┤
│                                          │
│ OPENAI_API_KEY=sk-...        ← SECRET   │
│ ANTHROPIC_API_KEY=sk-ant-... ← SECRET   │
│ GOOGLE_API_KEY=...           ← SECRET   │
│ GROQ_API_KEY=gsk_...         ← SECRET   │
│                                          │
│ ✅ Never exposed to client              │
│ ✅ Only accessible in API routes        │
│ ✅ Validated on startup                 │
│                                          │
└─────────────────────────────────────────┘
```

### Request Flow Security

```
CLIENT                    SERVER                  EXTERNAL API
  │                         │                         │
  │ POST /api/build        │                         │
  ├────────────────────────►│                         │
  │                         │                         │
  │                         │ Validates user auth    │
  │                         │ Checks API key         │
  │                         │                         │
  │                         │ generateCode()          │
  │                         ├────────────────────────►│
  │                         │                         │
  │                         │ ← Generated code        │
  │                         │◄────────────────────────┤
  │                         │                         │
  │ ← Streaming response   │                         │
  │◄────────────────────────┤                         │
  │                         │                         │

✅ API keys NEVER sent to client
✅ All AI calls happen server-side
✅ User authentication required
```

---

## 📊 Data Flow: Complete Build Process

```
┌──────────────────────────────────────────────────────────────┐
│                    1. User Submits Build                      │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│              2. Frontend → POST /api/build                    │
│  { prompt: "Todo app", userId: "abc123", buildId: "xyz" }    │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│              3. Create Firestore Document                     │
│  {                                                            │
│    id: "xyz",                                                 │
│    prompt: "Todo app",                                        │
│    status: "queued",                                          │
│    steps: [],                                                 │
│    createdAt: timestamp                                       │
│  }                                                            │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│           4. Frontend → GET /api/build/stream?id=xyz         │
│  Opens EventSource connection                                 │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│                5. AI Generation Starts                        │
│  const stream = generateApp({ prompt })                       │
│                                                               │
│  for await (const chunk of stream) {                          │
│    if (isStepMarker(chunk)) {                                 │
│      → Update Firestore                                       │
│      → Send to client via EventSource                         │
│    }                                                          │
│  }                                                            │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│              6. Real-time Updates                             │
│                                                               │
│  Firestore: steps = ["🔧 Init...", "📦 Installing..."]      │
│  Client receives via EventSource                              │
│  UI updates in real-time                                      │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│              7. Build Completes                               │
│  Update Firestore: status = "complete"                        │
│  Send "[DONE]" to client                                      │
│  Close EventSource connection                                 │
└──────────────────────────────────────────────────────────────┘
```

---

## 🎨 UI Component Hierarchy

```
VibelyBuild.AI
│
├── Layout (layout.js)
│   ├── Global Styles (globals.css)
│   │   ├── Bliss wallpaper background
│   │   ├── Glass card styles
│   │   └── Purple-blue gradients
│   │
│   └── TopNav (TopNav.jsx)
│       ├── Logo/Brand
│       ├── Navigation Tabs
│       │   ├── Build
│       │   ├── AI Ads
│       │   ├── Marketing
│       │   ├── Feed
│       │   ├── Store
│       │   └── Chat
│       │
│       └── User Section
│           ├── NotificationsBell
│           └── AuthButton (avatar only)
│
├── Pages
│   ├── Landing (page.js)
│   │   ├── Hero with prompt input
│   │   ├── How It Works (3 glass cards)
│   │   └── What You Can Build (8 categories)
│   │
│   ├── Build (build/page.js)
│   │   ├── Left: Prompt + My Builds
│   │   ├── Middle: Build Logs (streaming)
│   │   └── Right: Live Preview
│   │
│   ├── AI Ads (ads/page.js)
│   │   ├── Left: Form
│   │   └── Right: Generated Ads
│   │
│   ├── Marketing (marketing/page.js)
│   │   ├── KPI Dashboard
│   │   └── Campaign Manager
│   │
│   ├── Store (store/page.js)
│   │   └── App Grid (glass cards)
│   │
│   ├── Feed (feed/page.js)
│   │   ├── PostComposer
│   │   └── PostCard List
│   │
│   ├── Chat (chat/page.js)
│   │   ├── Conversation List
│   │   └── Message Thread
│   │
│   └── Profile (profile/page.js)
│       └── User Info + Apps
│
└── Shared Components
    ├── GlassWrapper (reusable glass container)
    ├── Feed Components (PostCard, etc.)
    └── Notification Components
```

---

## 🔄 State Management

```
┌─────────────────────────────────────────┐
│          Client State (Zustand)          │
├─────────────────────────────────────────┤
│                                          │
│  useUI Store                             │
│  ├── selectedBuildId                     │
│  ├── isPublishModalOpen                  │
│  ├── publishingBuildId                   │
│  └── isPublishing                        │
│                                          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         Server State (Firebase)          │
├─────────────────────────────────────────┤
│                                          │
│  Firestore Collections                   │
│  ├── users/{userId}                      │
│  │   ├── builds/{buildId}                │
│  │   ├── apps/{appId}                    │
│  │   └── ads/{adId}                      │
│  │                                        │
│  ├── posts/{postId}                      │
│  └── messages/{conversationId}           │
│                                          │
└─────────────────────────────────────────┘
```

---

## 📈 Scalability

### Current Capacity
- ✅ Handles multiple concurrent builds
- ✅ Real-time streaming to multiple clients
- ✅ Firebase auto-scaling
- ✅ Stateless API routes (easy to scale)

### Future Scaling Options
```
┌────────────────────────────────────────┐
│         Load Balancer                   │
├────────────────────────────────────────┤
│              ▼       ▼       ▼          │
│         ┌──────┐ ┌──────┐ ┌──────┐     │
│         │ API  │ │ API  │ │ API  │     │
│         │ Node │ │ Node │ │ Node │     │
│         └───┬──┘ └───┬──┘ └───┬──┘     │
│             └────────┼────────┘         │
│                      ▼                  │
│              ┌────────────┐             │
│              │  Firebase  │             │
│              └────────────┘             │
└────────────────────────────────────────┘
```

---

## 🎯 Summary

Your VibelyBuild.AI platform features a **clean, modular architecture** with:

✅ **Separation of concerns** - UI, API, AI system all isolated
✅ **Easy to maintain** - Change provider in one place
✅ **Scalable** - Stateless architecture, Firebase backend
✅ **Secure** - API keys server-side only
✅ **Extensible** - Add new providers easily
✅ **Production-ready** - Error handling, validation, logging

**Ready for:**
- 🚀 Production deployment
- 📈 User scaling
- 🔄 Feature additions
- 🎨 UI customization
